/*
 * Audacity: A Digital Audio Editor
 */
import QtQuick

//! See VideoPanelStub.qml - the import must resolve with video compiled out.
Item {
    property var navigationPanel
    property bool isCompactMode: false
}
