env:
  CIRRUS_CLONE_DEPTH: 1

freebsd_12_task:
  freebsd_instance:
    image: freebsd-12-1-release-amd64
  install_script:
    pkg install -y gettext gmake cmake wx31-gtk3 gtk3 git alsa-lib pkgconf
#  create_user_script:
#    - pw useradd git
#    - chown -R git:git .
  build_script:
    -  ls -la ;
       ln -s /usr/local/bin/wxgtk3u-3.1-config /usr/local/bin/wx-config ;
       ls -la /usr/local/bin ;
       set ;
       mkdir bld ;
       cd bld ;
       cmake .. ;
       make -j2 ;
       exit 0



