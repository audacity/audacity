name: '[AU4] Check: Codestyle'

on:
  pull_request:
  workflow_dispatch:

jobs:
  au4_codestyle:
    runs-on: ubuntu-latest
    steps:
    - name: Clone repository
      uses: actions/checkout@v6
      with:
        submodules: true

    - name: Test coding style
      run: |
        cmake -P ./muse_framework/buildscripts/ci/checkcodestyle/checkcodestyle.cmake ./src/
