; Seeded into the fresh CI profile by run_testflow_case.sh so that the UI
; renders deterministically (screenshot comparison). QSettings INI layout:
; the first key component is the section, the rest follows with '\'.

[ui]
application\currentThemeCode=light
application\followSystemTheme=false
theme\fontFamily=DejaVu Sans
theme\fontSize=12

[application]
checkForUpdate=false

[General]
language=en
